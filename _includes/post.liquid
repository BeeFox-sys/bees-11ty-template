{% layout "body.liquid" %}

{% block content %}
    <article>
        <h1 class="title">{{title}}</h1>
        <div class="date">{{date | date: "%Y-%m-%d"}}</div>
        <hr>
        {{content}}
    </article>
{% endblock %}